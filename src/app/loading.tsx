import LoadingButton from "@/components/loadingButton";

function Loading() {
  return (
    <div className="fixed inset-0 bg-zinc-100 flex items-center justify-center">
      <LoadingButton />
    </div>
  );
}

export default Loading;
